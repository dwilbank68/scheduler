<!DOCTYPE html>
<html>
<head>
  <title>Scheduler</title>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <!--<meta http-equiv="refresh" content="30">-->
</head>
<body>

<ul class='nav nav-tabs'>
  <li><%#= link_to "Bloccit", root_path %></li>
  <li><%#= link_to "About", about_path %></li>
  <li><%#= link_to "Topics", topics_path %></li>
  <li><%#= link_to "Popular", posts_path %></li>
  <div id="clock" class="quicksand-light white-glow-dim larger spaced"></div>
  <div id="timezone" class="quicksand-light white-glow-dim lowered spaced"><%= current_user.timezone if current_user %></div>
  <div id="new-unit">
    <%= link_to("New Unit", new_unit_path, class: 'quicksand-light white-glow-dim spaced') %>
  </div>

  <div class="pull-right user-info">
    <% if current_user %>
        <div class="quicksand-light spaced white-glow-dim" id="name-display"><%= current_user.name %></div>
        <%= image_tag(current_user.avatar.tiny.url) if current_user.avatar? %>
        <button id="<%= current_user.id %>" class="header-button quicksand-light spaced white-glow-dim">Edit Contact Preferences</button>
        <%= link_to edit_user_registration_path, class: 'btn btn-primary btn-sm' do %>
            <span class="glyphicon glyphicon-user"></span>
        <% end %>
        <%= link_to destroy_user_session_path, method: :delete, class: 'btn btn-primary btn-sm' do %>
            <span class="glyphicon glyphicon-log-out"></span>
        <% end %>
    <% else %>
        <%= link_to "Sign In", new_user_session_path %> or
        <%= link_to "Sign Up", new_user_registration_path %>
    <% end %>
  </div>

</ul>

    <div id="blur-layer">
      <%= yield %>
    </div>
    <p class="notice white-glow-dim microgramma spaced"><%= notice %></p>
    <p class="alert white-glow-dim microgramma spaced"><%= alert %></p>
    <div id="overlay"></div>

    <div id="non-blur-layer">
      <div class="container microgramma-bold spaced" id="user-edit-form">
        <div class="row">
          <div class="col-xs-12 spaced3-6 txt-centered"> <h2>Contact Information</h2> </div>
        </div>
        <% if current_user %>
            <%= simple_form_for current_user, defaults: {input_html: {class: 'quicksand-reg white-glow-dim', color: 'white'}} do |f| %>
                <div class="row"><div class="col-xs-12"><%= f.error_notification %></div></div>
                <div class="row border-bottom">
                  <div class="col-xs-1"></div>
                  <div class="col-xs-7"><%= f.input :name, label: false, placeholder: 'name', input_html: { :style => 'width: 260px'} %></div>
                </div>
                <div class="row border-bottom">
                  <div class="col-xs-1"><i class="fa fa-envelope-o"></i></div>
                  <div class="col-xs-7"> <%= f.input :email, label: false, placeholder: 'email', input_html: { :style => 'width: 260px' } %> </div>
                  <!--<div class="col-xs-2 quicksand-reg txt-right grayed-out email1-visible">visible</div> -->
                  <% if current_user.contact_flags[0] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow email1-visible">visible</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out email1-visible">visible</div>
                  <% end %>
                  <% if current_user.contact_flags[1] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow email1-active">active</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out email1-active">active</div>
                  <% end %>
                </div>
                <div class="row border-bottom">
                  <div class="col-xs-1"><i class="fa fa-envelope-o"></i></div>
                  <div class="col-xs-7"> <%= f.input :email2, label: false, placeholder: 'alternate email', input_html: {id: 'email2', :style => 'width: 260px'} %> </div>
                  <!--<div class="col-xs-2 quicksand-reg txt-right grayed-out email2-visible">visible</div> -->
                  <% if current_user.contact_flags[2] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow email2-visible">visible</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out email2-visible">visible</div>
                  <% end %>
                  <% if current_user.contact_flags[3] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow email2-active">active</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out email2-active">active</div>
                  <% end %>
                </div>
                <div class="row border-bottom">
                  <div class="col-xs-1"><i class="fa fa-phone-square"></i></div>
                  <div class="col-xs-7"> <%= f.input :phone, label: false, placeholder: 'phone', input_html: {id: 'phone', :style => 'width: 260px'} %> </div>
                  <!--<div class="col-xs-2 quicksand-reg txt-right grayed-out phone1-visible">visible</div> -->
                  <% if current_user.contact_flags[4] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow phone1-visible">visible</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out phone1-visible">visible</div>
                  <% end %>
                  <% if current_user.contact_flags[5] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow phone1-active">active</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out phone1-active">active</div>
                  <% end %>
                </div>
                <div class="row border-bottom">
                  <div class="col-xs-1"><i class="fa fa-phone-square"></i></div>
                  <div class="col-xs-7"> <%= f.input :phone2, label: false, placeholder: 'alternate phone', input_html: {id: 'phone2', :style => 'width: 260px'} %> </div>
                  <!--<div class="col-xs-2 quicksand-reg txt-right grayed-out phone2-visible">visible</div> -->
                  <% if current_user.contact_flags[6] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow phone2-visible">visible</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out phone2-visible">visible</div>
                  <% end %>
                  <% if current_user.contact_flags[7] == "1" %>
                      <div class="col-xs-2 quicksand-reg txt-right green-glow phone2-active">active</div>
                  <% else %>
                      <div class="col-xs-2 quicksand-reg txt-right grayed-out phone2-active">active</div>
                  <% end %>
                </div>
                <div class="row border-bottom">
                  <div class="col-xs-12"><%= f.input :timezone, :collection => ActiveSupport::TimeZone.all, :label_method => :to_s, :value_method => :name, :include_blank => false %></div>
                </div>
                <div class="spacer"></div>
                <div class="spacer"></div>
                <% if current_user.avatar? %>
                    <div class="row">
                      <div class="col-xs-6 current-avatar-label"> Current Avatar </div>
                      <div class="col-xs-6"> <%= image_tag( current_user.avatar.profile.url ) %> </div>
                    </div>
                <% end %>
                <div class="row border-bottom">
                  <div class="col-xs-6 microgramma spaced white-glow-dim"> <%= f.file_field :avatar %> </div>
                </div>
                    <%= f.hidden_field :avatar_cache %>
                    <%= f.input :contact_flags, :as => :hidden, :input_html => { :value => '', :id => 'contact-flags-' + current_user.id.to_s } %>
                <div class="row">
                  <div class="col-xs-12"> <%= f.error :base %> </div>
                </div>
                <div class="spacer"></div>
                <div class="txt-centered spaced"><%= f.button :submit, :class => 'update-user spaced' %></div>
                <div class="spacer"></div>
          <% end %>
        <% end %>
      </div>
    </div> <!-- where the modal form attaches -->


</body>
</html>


<script>

  $(document).ready(function() {

      $('#clock').jsclock(); //http://www.tcpweb.com.br/JS-Clock/

      var $user_form = $('#user-edit-form');
      $('#overlay').hide();
      $("#phone").intlTelInput();
      $("#phone2").intlTelInput();

//    the update user attributes form, triggered by "Edit Contact Preferences" in the upper right nav bar
      <% if current_user %>
          $('#<%= current_user.id %>').on('click', function(){
              if($('.signin').is(':visible')) {
                  console.log("not gonna show two forms at the same time, sir");
              } else {
                  // detach form from machine unit and reattach it to non-blurring layer, which is z-index 2
                  $('#non-blur-layer').append($user_form);
                  // blur the container which holds all the machines ( z-index 0)
                  $('#blur-layer').removeClass('blur-out');
                  $('#blur-layer').addClass('blur-in');
                  // bring in the tinted overlay to prevent click-through
                  $('#overlay').fadeIn();
                  // bring in the form, which SHOULD BE Z-INDEX 3
                  $user_form.fadeIn().css({top:($(window).height()/2)-107, left:($(window).width()/2)-200, zIndex:999});
              }
          });
      <% end %>

      // start all the countdowns on the units - showing time till unit is free
      startUnitCountdowns();
  });

  var startUnitCountdowns = function() {
      console.log('startUnitCountdowns is running');

      $.ajax({
          url: "/units/report_unit_statuses",
          type: "GET",
          dataType: "json",
          success: function(data) {
              unit_arr = data;
              for(var i=0; i<unit_arr.length; i++) {
//                console.log(unit_arr[i]);
                var id = unit_arr[i]['id'];
                var endTime = unit_arr[i]['time_available'];
                var endTimeMoment = moment(endTime);

                if(endTime === null) {
                  $('#unit_' + id).find($('.time-remaining')).html("--:--:--");
                } else {
                  $('#unit_' + id).find($('.time-remaining')).countdown({ until: endTimeMoment.toDate(), compact: true });
                }
              }
          },
          error: function() { console.log("ajax error in startUnitCountdowns") }
      });
  }

  // create an array of flags which tells which pieces of contact data are going to be active and visible
  // e.g., 0,1,1,0,0,0,1,1 or 1,1,1,1,0,0,0,0,

//  var contactFlags = "00000000".split('');
  var contactFlags = '<%= current_user.contact_flags %>'.split('');

  // set each element to 1 or 0 depending on which button user toggles

  $('.email1-visible').on('click', function(){

      if( contactFlags[0] == "1"){
          contactFlags[0] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[0] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });
  $('.email1-active').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[1] == "1"){
          contactFlags[1] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[1] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });
  $('.email2-visible').on('click', function(){
//      var formId = $(this).closest($('.unit_user_form')).attr('id');
//      var unitId = formId.replace( /^\D+/g, '');
      $(this).toggleClass('green-glow');
      if( contactFlags[2] == "1"){
          contactFlags[2] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[2] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });
  $('.email2-active').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[3] == "1"){
          contactFlags[3] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[3] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });

  $('.phone1-visible').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[4] == "1"){
          contactFlags[4] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[4] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });

  $('.phone1-active').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[5] == "1"){
          contactFlags[5] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[5] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });
  $('.phone2-visible').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[6] == "1"){
          contactFlags[6] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[6] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));

  });
  $('.phone2-active').on('click', function(){
      $(this).toggleClass('green-glow');
      if( contactFlags[7] == "1"){
          contactFlags[7] = "0";
          $(this).removeClass('green-glow');
          $(this).addClass('grayed-out');
      } else {
          contactFlags[7] = "1";
          $(this).removeClass('grayed-out');
          $(this).addClass('green-glow');
      }
      $('#contact-flags-' + <%= current_user.id %>).val(contactFlags.join(''));
  });



</script>